spring:
  profiles:
    active: local
    include: my
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-request-size: 30MB
      max-file-size: 10MB
com:
  example:
    upload:
      path: upload